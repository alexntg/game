<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        html, body {
            height: 100%;
            margin: 0;
            display: flex;
            flex-direction: column;
        }

        body {
            background-color: #f8f9fa;
        }

        header {
            width: 100%;
        }

        /* Navbar styles */
        .navbar {
            padding: 0.5rem 1rem;
        }
        .navbar-brand {
            width: 50%;
            text-align: center;
        }
        .navbar-nav {
            width: 50%;
            display: flex;
            justify-content: space-evenly;
        }
        .navbar-nav .nav-link {
            padding: 0.5rem;
        }
        .navbar-nav .logout-link {
            background: none; /* Sin fondo para que se vea como enlace */
            border: none; /* Sin borde */
            color: inherit; /* Hereda el color del texto del navbar */
            cursor: pointer; /* Cambia el cursor a puntero */
        }
        .navbar-nav .logout-link:hover {
            background-color: #e2e6ea; /* Color de fondo para hover */
            color: #000; /* Cambia el color del texto en hover */
        }

        /* Content area will stretch to fill the space between navbar and footer */
        main {
            flex: 1; /* Esto hace que el main ocupe el espacio restante entre navbar y footer */
            padding: 2rem 0;
        }

        /* Footer styles */
        footer {
            background-color: #343a40;
            color: #fff;
            padding: 1rem;
            text-align: center;
            width: 100%;
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="/">My Game</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/characters/select">Characters</a> 
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/game">Game</a>
                    </li>
                    <% if (isAuthenticated) { %> 
                        <li class="nav-item">
                            <form action="/logout" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-link nav-link">Logout</button>
                            </form>
                        </li>

                        <% if (isAdmin) { %> 
                            <li class="nav-item">
                                <a class="nav-link" href="/admin/edit">Editar</a>
                            </li>
                        <% } %>
                        <li class="nav-item">
                            <span class="nav-link">Welcome, <%= username %>!</span>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </nav>
    </header>

    <!-- Main content area -->
    <main class="container">
        <%- body %> 
    </main>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 My Game. All rights reserved.</p>
    </footer>

    <!-- Bootstrap JS and dependencies (jQuery and Popper.js) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
