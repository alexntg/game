<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'My Game' %></title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=UnifrakturMaguntia&display=swap" rel="stylesheet">
    
    <style>

        body {
            background-color: #121212; 
            color: #e0e0e0; 
            font-family: 'Cinzel', serif;
        }
        
        h1, p {
            font-family: 'UnifrakturMaguntia', cursive;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }

        h1 {
            font-size: 3rem;
        }
        
        p {
            font-size: 1.25rem;
        }

        .btn {
            border: 2px solid #9b870c;
            background-color: rgba(0, 0, 0, 0.6);
            color: #f8f9fa;
            text-shadow: 1px 1px 2px #000;
        }

        .btn-primary {
            background-color: #6c757d;
            border-color: #9b870c;
            transition: background-color 0.4s, box-shadow 0.4s;
        }

        .btn-primary:hover {
            background-color: #9b870c;
            box-shadow: 0 0 10px #f0a500, 0 0 20px #f0a500;
        }

        .btn-secondary {
            background-color: #343a40;
            border-color: #d4af37;
            transition: background-color 0.4s, box-shadow 0.4s;
        }

        .btn-secondary:hover {
            background-color: #d4af37;
            box-shadow: 0 0 10px #f0e68c, 0 0 20px #f0e68c;
        }

        .container {
            background-color: rgba(18, 18, 18, 0.85);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0px 0px 20px 5px rgba(0, 0, 0, 0.8);
        }

        .btn-lg {
            font-size: 1.25rem;
            font-family: 'Cinzel', serif;
        }
    </style>
</head>
<body>
    <div class="container text-center mt-5">
        <h1 class="mb-4">Welcome, Unkindled One</h1>
        <p class="mb-4">Choose your path, brave warrior:</p>

        <div class="d-grid gap-2 col-6 mx-auto">
            <a href="/characters/create" class="btn btn-primary btn-lg">Create Character</a>
            <a href="/characters/select" class="btn btn-secondary btn-lg">Select Character</a>
            <a href="/keybinds" class="btn btn-secondary btn-lg">Change Keybinds</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
