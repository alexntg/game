<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Character</title>
    
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Google Fonts for Souls-like feel -->
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=UnifrakturMaguntia&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #121212;
            color: #e0e0e0;
            font-family: 'Cinzel', serif;
        }

        h1 {
            font-family: 'UnifrakturMaguntia', cursive;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            font-size: 3rem;
            text-align: center;
            margin-bottom: 2rem;
        }

        label {
            color: #f0a500;
            font-weight: bold;
        }

        input[type="text"], input[type="number"] {
            background-color: rgba(0, 0, 0, 0.6);
            color: #f8f9fa;
            border: 1px solid #9b870c;
        }

        .btn-primary {
            background-color: #6c757d;
            border-color: #9b870c;
            color: #f8f9fa;
            font-size: 1.25rem;
            font-family: 'Cinzel', serif;
            transition: background-color 0.4s, box-shadow 0.4s;
        }

        .btn-primary:hover {
            background-color: #9b870c;
            box-shadow: 0 0 10px #f0a500, 0 0 20px #f0a500;
        }

        .container {
            background-color: rgba(18, 18, 18, 0.85);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0px 0px 20px 5px rgba(0, 0, 0, 0.8);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        a {
            color: #f8f9fa;
        }

        a:hover {
            color: #d4af37;
        }

        .form-control:focus {
            box-shadow: 0 0 10px #f0a500, 0 0 20px #f0a500;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1>Edit Character: <%= character.name %></h1>
        <form action="/characters/<%= character.id %>/update" method="POST">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" class="form-control" value="<%= character.name %>" required>
            </div>
            
            <div class="form-group">
                <label for="level">Level:</label>
                <input type="number" id="level" name="characterLevel" class="form-control" value="<%= character.characterLevel %>" required>
            </div>
            
            <div class="form-group">
                <label for="hp">HP:</label>
                <input type="number" id="hp" name="Hp" class="form-control" value="<%= character.Hp %>" required>
            </div>
            
            <div class="form-group">
                <label for="damage">Damage:</label>
                <input type="number" id="damage" name="Damage" class="form-control" value="<%= character.Damage %>" required>
            </div>
            
            <div class="form-group">
                <label for="stamina">Stamina:</label>
                <input type="number" id="stamina" name="Stamina" class="form-control" value="<%= character.Stamina %>" required>
            </div>

            <div class="text-center">
                <input type="submit" value="Update" class="btn btn-primary mt-3">
                <a href="/characters" class="btn btn-link mt-3">Back to Character List</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
